describe('Render app', () => {
    context('Render image container', () => {
        it('render the main image div', () => {});
        it('render the left arrow button', () => {});
        it('render the right arrow button', () => {});
        it('render the photo index indicator', () => {});
    });
    context('Render post layout', () => {
        context('Render post header', () => {
            it('render the profile picture', () => {});
            it('render the post owner username', () => {});
            it('render ellipsis', () => {});
        });
        context('Render post description', () => {
            it('render the profile picture', () => {});
            it('render the post owner username', () => {});
            it('render post description', () => {});
        });
        context('Render post reply area', () => {
            it('render the reaction icons bar', () => {});
            it('render the like by container    ', () => {});
        });
        context('Render post form', () => {
            it('render the input element', () => {});
            it('render the post comment button', () => {});
        });
    });
});
